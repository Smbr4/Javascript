const nome ='Luiz';//string
const nomeUm="Luiz";//string
const nomeDois=`Luiz`;//

const numUm= 10;//number
const numDois= 10.52;//number

let nomeAluno; //Variavel recebe o valor Undefined = nao aponta para lugar nenhum na memória
const sobrenomeAluno = null; //nulo -> nao apontam para lugar nenhum na memória. Usamos null quando queremos desconfigurar uma variável

const boolean = true; //boolean = tru, false (lógico), usamos boolean quando queremos fazer uma mudança no fluxo da aplicação

console.log(typeof const);